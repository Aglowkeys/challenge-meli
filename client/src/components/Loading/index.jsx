import SinglePageLayout from '../SinglePageLayout';
import Spinner from '../Spinner';

const Loading = () => {
    return (
        <SinglePageLayout>
            <Spinner />
        </SinglePageLayout>
    );
};

export default Loading;
